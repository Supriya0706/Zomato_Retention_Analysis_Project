services:
  - type: web
    name: zomato-retention-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /health
    autoDeploy: true
